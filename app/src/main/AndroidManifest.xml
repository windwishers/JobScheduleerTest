<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fail.toepic.eater.jobscheduleertest">

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    tools:ignore="GoogleAppIndexingWarning">
    <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <receiver
        android:name="fail.toepic.eater.jobscheduleertest.AlarmReceiver1"
        android:exported="true"
        tools:ignore="ExportedReceiver">
        <intent-filter>
            <action android:name="fail.toepic.eater.jobscheduleertest.AlarmTest" />
        </intent-filter>
    </receiver>

    <!--<receiver-->
        <!--android:name="fail.toepic.eater.jobscheduleertest.MainActivity$GlobalReceiver"-->
        <!--android:exported="true"-->
        <!--tools:ignore="ExportedReceiver">-->
        <!--<intent-filter>-->
            <!--<action android:name="fail.toepic.eater.jobscheduleertest.AlarmTest" />-->
        <!--</intent-filter>-->
    <!--</receiver>-->

    <service
        android:name="com.sk.pe.group.SimpleAlarm$JobService"
        android:permission="android.permission.BIND_JOB_SERVICE"
        android:exported="true"/>

</application>

</manifest>
